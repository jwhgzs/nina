// 这里将讲解Nina中的异常处理

// Nina中有 try 和 catch 语句来帮助你处理运行时异常~
// 请注意哦，这只能“帮助你处理运行时异常”，编译时就发现的离谱错误并不能捕获。
try {
    var a = null[1];
}
catch {
    // 通过特殊变量 exception 获取 try - catch 语句捕获到的异常信息：
    // 但是缺点是不能获取到具体的错误位置哦~
    console_printf('错误代号：' + exception.code);
    console_printf('错误信息：' + exception.message);
}
// 注意，在这里（catch 语句块外）无法访问到有效的特殊变量 exception ~
console_printf(exception);
// 输出 [Null]

// Nina中的 try - catch 并不是固定搭配哦~
// 例如，你可以这样单独使用 try 语句：
try {
    var a = null[1];
}